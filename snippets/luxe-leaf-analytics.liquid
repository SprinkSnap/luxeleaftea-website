{%- comment -%}
  Analytics tags are inlined in snippets/meta-tags.liquid
  (GA4 measurement ID + Google Search Console verification from theme settings).
  This file remains as a no-op for backward compatibility if still rendered elsewhere.
{%- endcomment -%}
